# Google Service Account Key (JSON string)
# This should contain the entire JSON key from your Google Service Account
# In GitHub Actions, this is stored as a secret
# For local testing, paste the JSON here (be careful not to commit!)
GOOGLE_SERVICE_ACCOUNT_KEY='{paste-your-service-account-json-here}'

# Whether to clean up temporary files after sync
# Set to 'false' to keep files for debugging
CLEANUP_TEMP=true